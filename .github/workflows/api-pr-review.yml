name: Lint Code
on: [pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ESLint
        run: |
          npm install eslint
          npx eslint . --format compact
